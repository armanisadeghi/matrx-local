-- matrx-local tool registry changeset
-- Applied through 2026-08-09 via Supabase MCP to project txzxabzwovsujtloxrus.
-- Verified: tool.definition.local_file advertises places, find, paged list,
-- and semantic_find; tool.definition.local_web advertises get_extraction at
-- the top level and in $variants.scrape. This tracked no-op prevents replay of
-- the applied UPDATEs.

BEGIN;
-- No pending tool registry changes.
COMMIT;
