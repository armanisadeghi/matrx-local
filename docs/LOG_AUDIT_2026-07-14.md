# Log Audit — 2026-07-14 12:27:26

Source: Matrx Log export pasted 2026-07-14. Deduped by message signature (timestamps, log channel `[tauri]` / `[server]` / `[syslog]` / `[access]`, and duplicate request dumps ignored).

Cross-repo items are noted where the failing system is not matrx-local.

---

## Summary

| ID | Severity | Signature | Repo |
|---|---|---|---|
| L001 | WARN | `[use-llm] Failed to notify engine of local LLM start` | matrx-local |
| L002 | WARN | `[app.launcher] app_config → degraded — running on cache config until remote fetch completes` | matrx-local |
| L003 | WARN | `[delegation] pending_calls unreachable — HTTP 500 SchemaMismatchError` | aidream (+ matrx-local client) |
| L004 | ERR | Download FAILED — `Tongyi-MAI--Z-Image-Turbo` — `huggingface_hub is not importable` | matrx-local |
| L005 | ERR | Download FAILED — `black-forest-labs--FLUX.1-schnell` — HF 401 gated repo | matrx-local |
| L006 | ERR | Download FAILED — `civitai--580857-2674760` — Civitai API key required/invalid | matrx-local |
| L007 | WARN | Download CANCELLED — `stabilityai--sdxl-turbo`, `Tongyi-MAI--Z-Image-Turbo` | matrx-local |
| L008 | WARN | `POST /v2/ai/agents/{id}` → 404 | matrx-local (+ matrx-frontend caller) |
| L009 | WARN | `POST /ai/agents/{id}` → 422 | matrx-local (+ matrx-frontend caller) |
| L010 | WARN | `GET /sandbox/fs/list?path=/{skill convention}` → 404 (6 paths) | aidream (+ matrx-local sandbox route) |

---

## L001 — Local LLM engine notification failed

**Log**

```
WARN [react] [use-llm] Failed to notify engine of local LLM start: {}
```

**When:** 12:01:11

**Emit site**

- [`matrx-local/desktop/src/hooks/use-llm.ts`](../../matrx-local/desktop/src/hooks/use-llm.ts) — `llm-server-ready` Tauri event handler calls `engine.connectLocalLlm(...)` and logs this warning in `.catch()`.

**Call chain**

1. Tauri fires `llm-server-ready`.
2. [`matrx-local/desktop/src/lib/api.ts`](../../matrx-local/desktop/src/lib/api.ts) `connectLocalLlm()` → `POST ${baseUrl}/chat/local-llm/connect`.
3. [`matrx-local/app/api/chat_routes.py`](../../matrx-local/app/api/chat_routes.py) `local_llm_connect()` → [`matrx-local/app/services/ai/local_llm_registry.py`](../../matrx-local/app/services/ai/local_llm_registry.py) `set_local_llm()`.

**Facts from code**

- `connectLocalLlm()` throws `new Error("Engine not discovered")` when `this.baseUrl` is falsy (line 453 in `api.ts`).
- The log shows `{}` because the caught value is an `Error` object; the React log channel does not serialize `Error.message` into the payload.

---

## L002 — App config boot degraded (cache tier)

**Log**

```
WARN [tauri] [app.launcher] [launcher] app_config → ⚠ degraded — running on cache config until remote fetch completes
```

**When:** 12:01:14 (also mirrored on `[server]`)

**Emit site**

- [`matrx-local/app/launcher.py`](../../matrx-local/app/launcher.py) — `ServiceRegistry.degraded()` logs `[launcher] %s → ⚠ degraded — %s`.
- Triggered from [`matrx-local/app/main.py`](../../matrx-local/app/main.py) Phase 00 (lines 306–308): immediately after synchronous cache/defaults resolution, before background remote fetch.

**Documented behavior**

- [`matrx-local/app/services/app_config/FEATURE.md`](../../matrx-local/app/services/app_config/FEATURE.md) — startup applies cache/defaults synchronously, marks registry `degraded(tier=…)`, then runs `start_background()` for the network fetch.

**Facts from log**

- Message states tier is `cache` (not `defaults` or `remote`).

---

## L003 — Delegation poll: aidream `pending_calls` HTTP 500

**Log**

```
WARN [delegation] pending_calls unreachable: [delegation] pending_calls → HTTP 500: {"error":"internal_error","message":"Internal server error: SchemaMismatchError",...,"request_id":"11de25f71af64401a3486587dd57ff62"}
```

**When:** 12:01:36

**Client emit site (matrx-local)**

- [`matrx-local/app/services/delegation/engine.py`](../../matrx-local/app/services/delegation/engine.py) line 197 — poll loop warning when `list_pending_calls()` raises.
- HTTP call: [`matrx-local/app/services/delegation/client.py`](../../matrx-local/app/services/delegation/client.py) `list_pending_calls()` → `GET {aidream}/ai/user/pending_calls` with optional `instance_id` query param.

**Server handler (aidream)**

- [`aidream/aidream/api/routers/conversations.py`](../../aidream/aidream/api/routers/conversations.py) `GET /user/pending_calls` → [`aidream/aidream/services/ai_execution/tool_results.py`](../../aidream/aidream/services/ai_execution/tool_results.py) `list_pending_calls()`.
- When `instance_id` is present, uses raw SQL in `_claim_pending_calls_for_instance()` (lines 389–449) referencing columns `target_instance_id`, `claimed_by_instance_id`, `claimed_at`, `claim_expires_at` on `chat.tool_call`.

**Schema definition**

- [`aidream/db/migrations/0172_targetable_desktop_tool_calls.sql`](../../aidream/db/migrations/0172_targetable_desktop_tool_calls.sql) adds those columns.

**Exception type**

- [`aidream/packages/matrx-orm/matrx_orm/exceptions.py`](../../aidream/packages/matrx-orm/matrx_orm/exceptions.py) `SchemaMismatchError` — raised when DB schema does not match the query (matrx-orm).

**Facts from log**

- HTTP 500 with `message: "Internal server error: SchemaMismatchError"`.
- matrx-local sends `instance_id` when resolvable ([`matrx-local/docs/TIER2_DESKTOP_INSTANCE_TARGETING.md`](../../matrx-local/docs/TIER2_DESKTOP_INSTANCE_TARGETING.md)).

---

## L004 — HF model download failed: AI packages not importable

**Log**

```
ERR [downloads] FAILED: id=f98e0567-12e3-4c0c-b217-0543eb293b41 file=Tongyi-MAI--Z-Image-Turbo error=huggingface_hub is not importable — the AI packages are not installed. Run the in-app installer (POST /image-gen/install) before downloading model weights. bytes_done=0 total=32848305533
```

**When:** 12:01:59

**Emit sites**

- UI log: [`matrx-local/desktop/src/contexts/DownloadManagerContext.tsx`](../../matrx-local/desktop/src/contexts/DownloadManagerContext.tsx) — logs `[downloads] FAILED` on `dm-failed` / failed status.
- Engine log: [`matrx-local/app/services/downloads/manager.py`](../../matrx-local/app/services/downloads/manager.py) — `_download_hf_snapshot()` and failure persistence.

**Failure origin**

- [`matrx-local/app/services/downloads/manager.py`](../../matrx-local/app/services/downloads/manager.py) lines 1137–1140: `from huggingface_hub import HfApi, hf_hub_download` inside `try/except ImportError` → raises `failures.ai_packages_missing()`.
- Current taxonomy: [`matrx-local/app/services/downloads/failures.py`](../../matrx-local/app/services/downloads/failures.py) `ai_packages_missing()` (code `ai_packages_missing`).

**Facts from log**

- Error text uses the pre-taxonomy message format (contains `POST /image-gen/install`).
- `bytes_done=0` — download did not start transferring bytes.
- Same failure id appears in persisted download state (also logged in [`matrx-local/CURRENT_ERRORS.md`](../CURRENT_ERRORS.md) from prior sessions).

**Boot ordering note (code comment)**

- [`matrx-local/app/main.py`](../../matrx-local/app/main.py) lines 444–446 document that download manager resuming HF jobs before image-gen sys.path injection produced false "AI packages are not installed" errors; Phase 0a reorder is documented in [`.matrx/AGENT_TASKS.md`](../.matrx/AGENT_TASKS.md).

---

## L005 — HF model download failed: gated repo 401

**Log**

```
ERR [downloads] FAILED: id=18add2b4-b22a-4d15-a752-033bb1ebae0b file=black-forest-labs--FLUX.1-schnell error=Hugging Face download failed for black-forest-labs/FLUX.1-schnell: 401 Client Error. ...
Cannot access gated repo for url https://huggingface.co/black-forest-labs/FLUX.1-schnell/resolve/main/model_index.json.
Access to model black-forest-labs/FLUX.1-schnell is restricted. ...
bytes_done=1 total=33725923002
```

**When:** 12:01:59

**Emit sites**

- Same download logging path as L004.
- HF API phase: [`matrx-local/app/services/downloads/manager.py`](../../matrx-local/app/services/downloads/manager.py) `_download_hf_snapshot()` → `HfApi.repo_info()` (lines 1154–1158).
- Failure classification: `_classify_hf_auth_failure()` and [`matrx-local/app/services/downloads/failures.py`](../../matrx-local/app/services/downloads/failures.py) (`hf_gate_not_accepted`, `hf_token_missing`, `hf_token_invalid`).

**Token source**

- [`matrx-local/app/services/media_gen/paths.py`](../../matrx-local/app/services/media_gen/paths.py) `read_hf_token()`.

**Facts from log**

- Repo: `black-forest-labs/FLUX.1-schnell`.
- HTTP 401 from Hugging Face; message states gated repo / authentication required.
- `bytes_done=1` — request reached HF before failing.

---

## L006 — Civitai download failed: API key

**Log**

```
ERR [downloads] FAILED: id=2f704646-7762-403b-9f28-78629fb645d2 file=civitai--580857-2674760 error=Civitai API key required or invalid — add your key under Settings → API Keys → Civitai, then retry the download. bytes_done=0 total=106
```

**When:** 12:01:59

**Emit sites**

- Same download UI/engine logging as L004.
- Civitai auth header: [`matrx-local/app/services/downloads/manager.py`](../../matrx-local/app/services/downloads/manager.py) lines 864–868 (`read_civitai_key()` from [`matrx-local/app/services/media_gen/paths.py`](../../matrx-local/app/services/media_gen/paths.py)).
- 401/403 classification: [`matrx-local/app/services/downloads/failures.py`](../../matrx-local/app/services/downloads/failures.py) `civitai_key_required()` / `civitai_key_rejected()` / `civitai_access_restricted()`.
- Pre-download inspect: [`matrx-local/app/services/image_gen/custom_models.py`](../../matrx-local/app/services/image_gen/custom_models.py) (Civitai HTTP error mapping).

**Facts from log**

- Error text matches pre-taxonomy blanket message documented in `failures.py` retriage comments (lines 282–283).
- `bytes_done=0`, `total=106`.

---

## L007 — Downloads cancelled

**Log**

```
WARN [downloads] CANCELLED: id=8dff0ba9-b10b-4d28-92f4-f9bd5fdd2553 file=stabilityai--sdxl-turbo
WARN [downloads] CANCELLED: id=e9efc7a8-68a4-4dbd-8893-4146553362f0 file=Tongyi-MAI--Z-Image-Turbo
```

**When:** 12:01:59

**Emit sites**

- UI: [`matrx-local/desktop/src/contexts/DownloadManagerContext.tsx`](../../matrx-local/desktop/src/contexts/DownloadManagerContext.tsx) — logs `[downloads] CANCELLED` on `dm-cancelled`.
- Engine: [`matrx-local/app/services/downloads/manager.py`](../../matrx-local/app/services/downloads/manager.py) — `cancel()` sets `status = "cancelled"` (line 549); HF worker checks `cancel_flag` (lines 1275–1279).
- Rust-side manager also emits `dm-cancelled`: [`matrx-local/desktop/src-tauri/src/downloads/manager.rs`](../../matrx-local/desktop/src-tauri/src/downloads/manager.rs).

**Facts from log**

- Two distinct download ids/files cancelled in the same second as the three FAILED entries (L004–L006).
- `Tongyi-MAI--Z-Image-Turbo` appears in both FAILED (L004) and CANCELLED entries (different ids).

---

## L008 — Agent start: `/v2/ai/agents` → 404

**Log**

```
WARN POST /v2/ai/agents/8d02d271-f007-4db3-90f2-3cc596190db0 → 404 (3ms)
```

**When:** 12:05:10 (logged on `[tauri]`, `[access]`, `[syslog]`, `[server]`)

**Request facts from log**

- Target: `http://127.0.0.1:22140/v2/ai/agents/8d02d271-f007-4db3-90f2-3cc596190db0`
- Origin: `https://www.aimatrx.com`
- Body includes `is_new: true`, `conversation_id: 82dd8dd2-9f6c-4496-8c09-d02164bc84ee`, sandbox-fs + desktop-native client capabilities, tools list (`fs_read`, `fs_write`, …).

**Caller path (matrx-frontend)**

- [`matrx-frontend/lib/api/ai-api-version.ts`](../../matrx-frontend/lib/api/ai-api-version.ts) — `toV2Path()` prepends `/v2` for covered AI routes including `/ai/agents/{agent_id}`.

**Server mount (matrx-local)**

- [`matrx-local/app/main.py`](../../matrx-local/app/main.py) Phase 1b mounts `build_ai_app()` at `/ai` and `/chat/ai` only — no `/v2/ai` mount in matrx-local.
- [`matrx-local/app/api/ai_routes.py`](../../matrx-local/app/api/ai_routes.py) defines routes under `/agents/{agent_id}` relative to the `/ai` mount.

**Aidream reference (cloud server has v2)**

- [`aidream/aidream/api/routers/v2.py`](../../aidream/aidream/api/routers/v2.py) — `POST /ai/agents/{agent_id}` on the spine (aidream only).

**Access log emit site**

- [`matrx-local/app/main.py`](../../matrx-local/app/main.py) `_log_requests_dispatch()` — logs `← %d %s %s` at WARNING for status ≥ 400.

---

## L009 — Agent start: `/ai/agents` → 422

**Log**

```
WARN POST /ai/agents/8d02d271-f007-4db3-90f2-3cc596190db0 → 422 (8ms)
```

**When:** 12:05:10 (immediately after L008 fallback)

**Handler**

- [`matrx-local/app/api/ai_routes.py`](../../matrx-local/app/api/ai_routes.py) `start_agent()` → [`matrx-local/app/services/ai/local_ai_task.py`](../../matrx-local/app/services/ai/local_ai_task.py) `prepare_agent_start()`.

**422 emit paths in `local_ai_task.py` (log does not include response body)**

| Code | Condition | Lines |
|---|---|---|
| `invalid_uuid` | Malformed UUID fields | 77–83 |
| `agent_model_missing` | Agent cached settings have no model and request has no `config_overrides.model` | 306–316 |
| `tool_not_found` | Request `tools` / `tools_replace` names a registered tool absent from local `ToolRegistry` | 191–202 |

**Facts from log request body**

- Injected tools: `fs_read`, `fs_write`, `fs_edit`, `fs_patch`, `fs_list`, `fs_mkdir`, `fs_search`, `shell_execute`, `shell_python`, `git_ingest` (all `kind: registered`).
- matrx-local OS tool catalog ([`matrx-local/app/tools/catalog.py`](../../matrx-local/app/tools/catalog.py)) registers `local_*` cloud names — not the sandbox `fs_*` / `shell_execute` / `git_ingest` names from matrx-ai's sandbox capability set ([`aidream/packages/matrx-ai/matrx_ai/capabilities/built_in.py`](../../aidream/packages/matrx-ai/matrx_ai/capabilities/built_in.py)).

**404 path (same handler, not triggered in this log line)**

- `_load_local_agent()` → 404 `agent_not_found` when agent id absent from local SQLite ([`matrx-local/app/services/ai/local_ai_task.py`](../../matrx-local/app/services/ai/local_ai_task.py) lines 247–254).

---

## L010 — Skill auto-discovery: sandbox fs/list 404 for convention paths

**Log (one signature, six paths)**

```
WARN GET /sandbox/fs/list?path=%2F.claude%2Fskills&recursive=false&depth=1 → 404
WARN GET /sandbox/fs/list?path=%2F.cursor%2Fskills&recursive=false&depth=1 → 404
WARN GET /sandbox/fs/list?path=%2F.agent%2Fskills&recursive=false&depth=1 → 404
WARN GET /sandbox/fs/list?path=%2F.agents%2Fskills&recursive=false&depth=1 → 404
WARN GET /sandbox/fs/list?path=%2F.matrx%2Fskills&recursive=false&depth=1 → 404
WARN GET /sandbox/fs/list?path=%2Fskills&recursive=false&depth=1 → 404
```

**When:** 12:07:16–12:07:18

**Request facts from log**

- Proxied URL host: `mic-limit-nutten-designation.trycloudflare.com` (Cloudflare tunnel to matrx-local).
- `user-agent: python-httpx/0.28.1` (aidream server-side caller).
- Paths are absolute from sandbox `root_path: "/"` (from the 12:05:10 agent request body).

**Discovery caller (aidream)**

- [`aidream/aidream/services/sandboxes/sandbox_autobind.py`](../../aidream/aidream/services/sandboxes/sandbox_autobind.py) `_auto_discover_skills()` — background task after sandbox bind.
- [`aidream/packages/matrx-ai/matrx_ai/skills/ingest.py`](../../aidream/packages/matrx-ai/matrx_ai/skills/ingest.py) `walk_via_proxy()` — iterates `_SKILL_DIR_CONVENTIONS` (lines 327–334), calls `fs_list()` per convention (lines 582–587).

**HTTP proxy**

- [`aidream/packages/matrx-ai/matrx_ai/tools/_sandbox_proxy.py`](../../aidream/packages/matrx-ai/matrx_ai/tools/_sandbox_proxy.py) `fs_list()` → `GET /fs/list`.

**Route handler (matrx-local)**

- [`matrx-local/app/api/sandbox_routes.py`](../../matrx-local/app/api/sandbox_routes.py) `fs_list()` — mounted at `/sandbox` via [`matrx-local/app/main.py`](../../matrx-local/app/main.py) line 1495.
- Returns HTTP 404 `path_not_found` when `_resolve_path(path)` does not exist (lines 250–251).

**Ingest behavior on 404**

- `walk_via_proxy()` catches `SandboxProxyError` with `status == 404` and continues silently (lines 588–595) — 404 is treated as "convention not present."

**Facts from log**

- All six convention probes returned 404; access middleware logged each at WARNING ([`matrx-local/app/main.py`](../../matrx-local/app/main.py) `_log_requests_dispatch()`).

---

## Agent tasks (scheduled)

Tasks filed in [`.matrx/AGENT_TASKS.md`](../.matrx/AGENT_TASKS.md) under **Active** — IDs **L001–L010**. Each task is investigate/fix scoped to the source files listed above. No solution is specified in this audit; tasks reference this document only.

| Task ID | Log ID | Priority | Scope |
|---|---|---|---|
| AT-L001 | L001 | P2 | matrx-local — local LLM connect failure at boot |
| AT-L002 | L002 | P3 | matrx-local — app_config boot degraded visibility |
| AT-L003 | L003 | P1 | aidream — `pending_calls` SchemaMismatchError (migration 0172) |
| AT-L004 | L004 | P2 | matrx-local — Z-Image download / AI packages import |
| AT-L005 | L005 | P2 | matrx-local — FLUX gated HF download state |
| AT-L006 | L006 | P2 | matrx-local — Civitai download auth state |
| AT-L007 | L007 | P3 | matrx-local — concurrent cancel + fail on same models |
| AT-L008 | L008 | P1 | matrx-local + matrx-frontend — `/v2/ai` on local engine |
| AT-L009 | L009 | P1 | matrx-local + matrx-frontend — local agent POST 422 |
| AT-L010 | L010 | P3 | aidream + matrx-local — skill discovery 404 log noise |

---

## Related existing tracking

- [`matrx-local/CURRENT_ERRORS.md`](../CURRENT_ERRORS.md) — prior download failure states (same three failed files).
- [`matrx-local/docs/TIER2_DESKTOP_INSTANCE_TARGETING.md`](TIER2_DESKTOP_INSTANCE_TARGETING.md) — delegation `instance_id` contract (L003).
- [`matrx-local/docs/handoffs/00-INTEGRATION-TRACKER.md`](handoffs/00-INTEGRATION-TRACKER.md) — W7 delegation alignment (L003 context).
